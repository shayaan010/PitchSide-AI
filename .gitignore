# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Environment
.env

# Data — the prebuilt index ships with the repo so deploys have a corpus.
# Rebuild with `python backend/seed.py` or POST /ingest, then re-commit.
*.sqlite3-wal
*.sqlite3-shm
data/articles.db-wal
data/articles.db-shm

# Node
frontend/node_modules/
frontend/dist/

# OS
.DS_Store
Thumbs.db

# Local env (never commit secrets here)
.env.local
.env.*.local
